using namespace std;
#include <iostream> // input output stream

int main() // entry point of the application
{
	/*int age = 30;
	float money = 145.202; // 4 bytes
	double scientific = 15162654681515975.646168; // 8 bytes

	char name = 'Y';
	char name1[8] = "yassine"; // Y A S S I N E `EOL` 'end of line'
	string name2 = "ZAIRI";

	bool isAngry = false;
	cout << isAngry;*/
	////////////////////////////////////////////////////////////////////
	/*int x;
	int y;
	string name;

	cout << "Enter the first number:";
	cin >> x;
	cout << "Enter the second number:";
	cin >> y;
	cout << "The sum of the numbers is:\n" << x + y;


	cout << "Enter your name:\n";
	cin >> name;
	cout << "The name you entered is: " << name;*/
	////////////////////////////////////////////////////////////////////
	/*				
	int x = 2;
	int y = 3;

	int sum = x + y;
	cout << "Sum = " << x + y << endl;

	int difference = x - y;
	cout << "subtraction = " << difference << endl;

	int product = x * y;												
	cout << "Product = " << product << endl;

	double division = x / y;
	cout << "division = " << division << endl;

	int remainder = x % y;																		
	cout << "Remainder = " << remainder << endl;*/
	////////////////////////////////////////////////////////////////////
	/*int grade;
	cout << "Enter your grade:" << endl;
	cin >> grade;
	if (cin.fail())
		cout << "Bad input!" << endl;
	else if (grade > 100 || grade < 0)
		cout << "Enter a valid number!" << endl;
	else if (grade >= 90)
		cout << "A+" << endl;
	else if (grade >= 80)
		cout << "B+" << endl;
	else if (grade >= 70)
		cout << "C+" << endl;
	else if (grade >= 60)
		cout << "D+" << endl;
	else if (grade >= 50)
		cout << "D" << endl;
	else if (grade < 50)
		cout << "F" << endl;*/
	/////////////////////////////////////////////////////////////////////
	/*int num;
	cout << "Enter a number: " << endl;
	cin >> num;

	if (num % 2 == 0)
		cout << "The number is even!";
	else
		cout << "The number is odd!";*/
	/////////////////////////////////////////////////////////////////////
		//Even or Odd

		/*int age;
		cout << "Enter your age: ";
		cin >> age;
		if (age >= 65)
			cout << "You are a senior citizen eligible to vote!";
		else if (age >= 18) {
			cout << "You are eligible to vote!" << endl;
		}
		else {
			cout << "You are NOT eligible to vote." << endl;
		}*/
	/////////////////////////////////////////////////////////////////////
		//Check for Voting Eligibility

		/*int a, b, c;
		cout << "Enter three numbers: ";
		cin >> a >> b >> c;
		if (a == b && a == c && b == c)
			cout << "The numbers are equal!";
		else if (a >= b && a >= c) {
			cout << "The largest number is: " << a << endl;
		}
		else if (b >= a && b >= c) {
			cout << "The largest number is: " << b << endl;
		}
		else {
			cout << "The largest number is: " << c << endl;
		}*/
/////////////////////////////////////////////////////////////////////////
		//Grade Calculator

		/*int score;
		cout << "Enter your exam score (0-100): ";
		cin >> score;
		if (score > 100 || score < 0)
			cout << "Enter a valid grade!" << endl;
		else if (score >= 90) {
			cout << "Your grade is: A" << endl;
		}
		else if (score >= 80) {
			cout << "Your grade is: B" << endl;
		}
		else if (score >= 70) {
			cout << "Your grade is: C" << endl;
		}
		else if (score >= 60) {
			cout << "Your grade is: D" << endl;
		}
		else {
			cout << "Your grade is: F" << endl;
		}*/
////////////////////////////////////////////////////////////////////////
		//Simple ATM System

		/*int PIN;
		cout << "Enter your PIN:" << endl;
		cin >> PIN;

		if (PIN == 123)
			cout << "Access granted!" << endl;
		else
			cout << "Access denied.Try again!" << endl;*/
////////////////////////////////////////////////////////////////////////
}


